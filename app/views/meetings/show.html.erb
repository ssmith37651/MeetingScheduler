<h1>View Meeting's <%= pluralize(@participants.count, "Participant") %></h1>
<table>
	<thead>
		<tr>
			<th>First Name</th>
			<th>Last Name</th>
			<th>Email Address</th>
		</tr>
	</thead>
	<tbody>
		<% @participants.each do |participant| %>
		<tr>
			<td><%= participant.first_name %></td>
			<td><%= participant.last_name %></td>
			<td><%= participant.email_address %></td>		
		</tr>
		<% end %>
	</tbody>
</table>
<a href="/meetings">View Meetings</a>
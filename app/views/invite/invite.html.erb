<h1 class="text-white".text-white>Invite Participants</h1>
<form action="/invite" method="post">
	<input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
	<p>
		<label>Name:</label>
		<select name="invite[participant_id]" id="participant_id">
			<option></option>
			<% @participants.each do |participant| %>
			<option value="<%= participant.id %>">
				<%= participant.first_name %> <%= participant.last_name %>
			</option>
			<% end %>
		</select>
	</p>
	<p>
		<label>Email Address:</label>
		<select name="invite[participant_id]" id="participant_id">
			<option></option>
			<% @participants.each do |participant| %>
			<option value="<%= participant.id %>">
				<%= participant.email_address %>
			</option>
			<% end %>
		</select>
	</p>
	<p>
		<label>Meeting:</label>
		<select name="invite[meeting_id]" id="meeting_id">
			<option></option>
			<% @meetings.each do |meeting| %>
			<option value="<%= meeting.id %>">
				<%= meeting.meeting_name %>
			</option>
			<% end %>
		</select>
	</p>
	<input class="btn btn-primary" type="submit" value="Invite">
</form> 

<!-- <form action="/invite" method="post">
	<input name="invite[participant_id]" id="participant_id" type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
	<div class="pb-4 form-row">
		<div class="col-2">
			<input name="invite[participant_id]" id="participant_id" type="text" class="form-control" placeholder="First name">
		</div>
		<div class="col-2">
			<input type="text" class="form-control" placeholder="Last name">
		</div>
	</div>
	<div class="pb-4 form-row">
		<div class="col-3">
			<input type="email" class="form-control" name="invite[meeting_id]" id="meeting_id" aria-describedby="emailHelp" placeholder="E-mail">
		</div>
	</div>
	<div class="pb-4 form-row align-items-center">
		<div class="col-auto my-1">
			<label class="mr-sm-2 sr-only" for="participant_id">Preference</label>
			<select class="custom-select mr-sm-2" id="participant_id">
				<option selected>Choose...</option>
				<% @meetings.each do |meeting| %>
				<option value="<%= meeting.id %>">
					<%= meeting.meeting_name %>
				</option>
				<% end %>
			</select>
		</div>
	</div>
	<input class="btn btn-primary" type="submit" value="Invite">
</form> -->

<% if flash[:errors] %>
	<% flash[:errors].each do |e| %>
	<p><%= e %></p>
	<% end %>
<% end %>
